# Lições

> **Arquivo renderizado por `licoes.py`. Não editar à mão.**
> O estado canônico é `.specs/memory/licoes.json`.
>
> ```
> python3 <skill-dir>/scripts/licoes.py registrar \
>   --contexto <feature/task> --o-que "<o que aconteceu>" --licao "<o que aprendemos>"
> python3 <skill-dir>/scripts/licoes.py listar --estado confirmada
> python3 <skill-dir>/scripts/licoes.py promover L-007 --para .specs/rules/evidencia.md
> ```
>
> **Graduação automática:** a mesma lição registrada numa segunda feature distinta vira
> `confirmada`. Confirmada vira regra em `.specs/rules/` (processo) ou em `AGENTS.md`
> (código), e sai daqui como referência.

*(vazio até a primeira lição)*
